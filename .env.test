# Test Environment Variables
NODE_ENV=test

# Test API Keys (these are not real, just for testing)
GEMINI_API_KEY=test-gemini-api-key
STRIPE_SECRET_KEY=sk_test_mock_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret
RESEND_API_KEY=re_test_resend_api_key

# Test Database (use in-memory for tests)
DATABASE_URL=postgresql://test:test@localhost:5432/test_db

# Session Secret
SESSION_SECRET=test-session-secret-key-for-testing-only

# Object Storage (mock for tests)
REPLIT_OBJECT_STORAGE_BUCKET=test-bucket

# Test URLs
FRONTEND_URL=http://localhost:5000
BACKEND_URL=http://localhost:5000

# Rate limiting (disabled for tests)
RATE_LIMIT_ENABLED=false
